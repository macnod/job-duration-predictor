apiVersion: v1
kind: Service
metadata:
  name: predictor-service
  namespace: predictor
  labels:
    app: predictor-service
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
  selector:
    app: predictor-api
